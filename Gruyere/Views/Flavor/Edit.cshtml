@{
  Layout = "_Layout";
}
<div class="container">
  @await Html.PartialAsync("_Navbar")
  @model Gruyere.Models.Flavor
  <div class="card card-body">
    <h5 class="card-title">Editing: @Model.Name</h5>
    <form action="~/Flavor/Edit" method="post">
      @Html.HiddenFor(model => model.FlavorId)
      <div class="form-group">
        @Html.TextBoxFor(model => model.Name, new { @class="form-control required", @required="required", @placeholder="{Model.Name}"})
      </div>
      <div class="form-group">
        @Html.DropDownList("TreatId", (SelectList)ViewBag.TreatId, "Select treat", new { @class = "form-control" })
      </div>
      <button type="submit" class="btn btn-primary">Update</button>
      <a>@Html.ActionLink("Cancel", "Index", "Flavor", null, new { @class="btn btn-danger"})</a>
    </form>
  </div>
  @await Html.PartialAsync("_Footer")
</div> 