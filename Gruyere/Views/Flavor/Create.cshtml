@{
  Layout = "_Layout";
}
<div class="container">
  @await Html.PartialAsync("_Navbar")
  @model Gruyere.Models.Flavor
  <div id="addFlavor">
    <div class="card card-body">
      <h5 class="card-title">Add a new Flavor</h5>
      <form action="~/Flavor/Create" method="post">
        <div class="form-group">
          @Html.TextBoxFor(model => model.Name, new { @class="form-control required", @required="required", @placeholder="Vanilla"})
        </div>
        <button type="submit" class="btn btn-primary">Add Flavor</button>
        <a>@Html.ActionLink("Cancel", "Index", "Flavor", null, new { @class="btn btn-danger"})</a>
      </form>
    </div>
    @await Html.PartialAsync("_Footer")
  </div>
</div>